export const CREDITOR_VALIDATION_COPY = {
  choice: 'Select a creditor',
  major: 'Select a major creditor',
} as const;

export const CREDITOR_STATUS_COPY = {
  loading: 'Loading major creditors…',
  empty: 'No major creditors are available. Choose another creditor or try again.',
} as const;
